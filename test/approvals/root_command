stash - Bidirectionally sync Markdown files with Apple Notes

Usage:
  stash COMMAND
  stash [COMMAND] --help | -h
  stash --version | -v

Commands:
  push          Push a Markdown file to Apple Notes (create or update)
  pull          Pull content from Apple Notes to a Markdown file
  diff          Show differences between a Markdown file and its linked Apple Note
  completions   Generate bash completions
